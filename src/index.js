import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,  document.getElementById('root')
    // está colocando o App dentro de uma div root que está no index.html da pasta public
);
